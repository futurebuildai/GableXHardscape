# Leave VITE_API_URL empty so requests use relative paths and go through the
# vite dev-server proxy (configured in vite.config.ts). The proxy targets
# VITE_API_PROXY (defaults to http://localhost:8080) and rewrites /api/v1/*
# correctly for routes mounted at the backend root.
VITE_API_URL=
